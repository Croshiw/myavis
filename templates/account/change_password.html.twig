{% extends 'layouts/base.html.twig' %}

{% block title 'Changer le mot de passe' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto text-center">
            <h1 class="text-center my-5">Changer le mot de passe</h1>

            {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="d-grid gap-2 mt-3">
                    <button type="submit" class="btn btn-primary btn-lg" formnovalidate>Valider le mot de passe</button>
                </div>
            {{ form_end(form) }}
        </div>
        
    </div>
{% endblock %}